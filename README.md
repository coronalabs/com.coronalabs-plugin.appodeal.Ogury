Sources for the plugin `plugin.appodeal.Ogury`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Ogury" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
